spring:
  datasource:
    #    hikari:
    #      minimum-idle: 5
    #      maximum-pool-size: 20
    #      idle-timeout: 10000
    #      max-lifetime: 1800000
    #      connection-timeout: 20000
    url: jdbc:mysql://localhost:3306/saleapp?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
#    properties:
#      hibernate:
#        format_sql: true
